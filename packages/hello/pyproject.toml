[build-system]
requires = ["uv==0.5.29"]
build-backend = "uv"

[project]
name = "hello"
version = "0.0.0"
dependencies = ["cappa>=0.26.3"]
[project.scripts]
"hello" = "hello.__main__:main"

[tool.fawltydeps]
deps = ["src/hello/pyproject.toml"]
code = ["src/hello"]
